import 'package:flutter/material.dart';

class ListRestaurantErrorWidget extends StatelessWidget {
  const ListRestaurantErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Lỗi"),
    );
  }
}
